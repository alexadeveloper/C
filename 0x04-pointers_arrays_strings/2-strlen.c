#include "holberton.h"

/**
  *_strlen - returns the length of a string
  *@s: is a char
  *Return: the length of a string
 */

int _strlen(char *s)
{
	int b;
	char a[] = {s};

	b = sizeof(a);
return (b);
}
